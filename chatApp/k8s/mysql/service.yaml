apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: chatapp
spec:
  clusterIP: None  # headless — StatefulSet pods get stable DNS names
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
