apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: chatapp
spec:
  clusterIP: None  # headless — stable DNS for StatefulSet
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
